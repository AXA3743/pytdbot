from setuptools import setup, find_packages

setup(
    name='pytdbot',
    version='0.1',
    description='Simple tdlib wrapper for Telegram bot using python',
    author='AXA3743',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
